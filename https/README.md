# https

HTTPS for PSP homebrew: wolfSSL with TLS 1.3, bundled CAs, and a question to the person instead of a failure when a certificate is only expired or from an unknown issuer.

Empty for now; the code comes out of PSPDX.
